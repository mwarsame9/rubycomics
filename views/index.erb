<h1>test</h1>

<h3>All Pages</h3>
  <ul>
    <% @pages.each() do |page| %>
    <!-- TODO: change this to a thumbnail later -->
      <li class="page"><a href="/pages/<%= page.id %>"><img src="<%= page.page_img.url %>" alt="comic page" class="img-thumbnail" width="50" height="40"></a></li>
    <% end %>
  </ul>
